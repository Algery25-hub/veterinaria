export default class Mascota {
  constructor(nombre, especie, edad, propietario) {
    this.nombre = nombre;
    this.especie = especie;
    this.edad = edad;
    this.propietario = propietario;
  }
}